# Keep MCP config and Stitch API key out of version control
.cursor/mcp.json
.cursor/stitch-api-key
.cursor/node_modules

# Production notes (DB credentials, env reference) — never commit
env.production.notes
